# raw 리소스 안내

`music.mp3` 는 저작권 문제로 저장소에서 제외되어 있습니다.

이 프로젝트를 빌드하려면 임의의 mp3 파일을 이 폴더에 **`music.mp3`** 라는 이름으로 넣으세요.

```
Android_Multimedia/app/src/main/res/raw/music.mp3
```

파일이 없으면 `R.raw.music` 을 참조하는 코드에서 컴파일 오류가 납니다.
